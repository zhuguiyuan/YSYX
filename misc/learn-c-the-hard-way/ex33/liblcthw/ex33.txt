-4. 实现了 inplace 版本，消耗输入的参数的生命周期，产出一新的有序列表。

-3. 优化是在元素比较少的时候使用插入排序或者其他内存交换小的排序来 cache 优化。

-2. 没有办法用 List_join，可以 List_split，详细见 `list_algos.c`。

-1. 实现了一个 invariant_check 宏，在没有定义 NDEBUG 的时候检查不变量。

1. 2. 3. 实际上是 man 2 time。详细见 `tests/list_algo_time_tests.c`。

4. 第一个是不能随机访问；第二个是链表的节点操作很麻烦；第三个是会涉及到非常多小对象分配。

5.

6.
